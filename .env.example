# NOTE: This app is a pure frontend app — do NOT commit real API keys.
# The app now securely prompts the user for their API key via the Chatbot UI.
# The key is stored locally in the user's browser (localStorage).

# Get your free Gemini API key at: https://aistudio.google.com/app/apikey
